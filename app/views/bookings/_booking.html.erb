      <div class="card-trip">
        <%= cl_image_tag booking.rocket.photo.key, height: 200, width: 200, crop: :fill %>
        <div class="card-trip-infos">
          <div>
            <h1>Recap of your booking :</h1>
            <h3>Name of the Rocket that you booked : <%= booking.rocket.name %></h3>
             <h3>Start date : <%= booking.start_date %></h3>
             <h3>End date : <%= booking.end_date %></h3>
             <h3>Total duration of your trip : <%= booking.end_date - booking.start_date + 1 %></h3>
            <h3 class="rocket-show-destination">Going to : <%= booking.rocket.destination %></h3>
          </div>
          <h3 class="card-trip-pricing">Total StarCoins : <%= booking.price %></h3>
          <h2><%= cl_image_tag booking.rocket.user.photo.key, height: 40, width: 40, radius:'max' %>
        </div>
      </div>
